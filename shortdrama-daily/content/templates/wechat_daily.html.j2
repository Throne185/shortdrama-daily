<!-- WeChat-friendly inline HTML: Daily Short-Drama Digest (Mobile-first) -->
<div style="font-family:-apple-system,BlinkMacSystemFont,Segoe UI,Roboto,Helvetica,Arial,PingFang SC,Microsoft Yahei,Helvetica Neue;font-size:17px;line-height:1.8;color:#222;max-width:760px;margin:0 auto;">
  <h2 style="margin:0 0 10px;font-size:22px;line-height:1.4;">{{ title }}</h2>
  <div style="color:#888;font-size:13px;margin-bottom:14px;">每日短剧热榜 · {{ date }} · 来源：各平台公开页面</div>

  <div style="background:#fff6f0;border-left:4px solid #ff7a45;padding:10px 12px;margin-bottom:14px;border-radius:4px;">
    <strong>一句话爽点：</strong>{{ hook }}
  </div>

  <h3 style="margin:16px 0 8px;font-size:18px;">今日热榜 TOP {{ items|length }}</h3>

  {% for it in items %}
  <div style="margin:0 0 14px;border:1px solid #eee;border-radius:8px;overflow:hidden;background:#fff;">
    <img src="{{ it.poster_local or it.poster }}" alt="短剧海报" style="width:100%;height:auto;display:block;object-fit:cover;">
    <div style="padding:10px 12px;">
      <div style="font-weight:600;margin-bottom:6px;">《{{ it.title }}》 <span style="font-size:12px;color:#999;margin-left:6px;">{{ it.platform }} · {{ it.status or '更新中' }}</span></div>
      <div style="margin:0 0 8px;">
        {% for tag in it.tags %}
        <span style="display:inline-block;background:#e6f7ff;color:#08979c;font-size:12px;padding:2px 6px;border-radius:4px;margin-right:6px;margin-bottom:4px;">{{ tag }}</span>
        {% endfor %}
      </div>
      <div style="color:#444;">剧情亮点：{{ it.highlight }}</div>
      {% if it.comment %}
      <blockquote style="margin:8px 0 0;padding:8px 10px;background:#fafafa;border-left:3px solid #d9d9d9;color:#666;font-size:14px;border-radius:4px;">
        网友说：“{{ it.comment.content }}” <span style="color:#999;font-size:12px;">— {{ it.comment.source }}</span>
      </blockquote>
      {% endif %}
    </div>
  </div>
  {% endfor %}

  {% if upcoming|length > 0 %}
  <h3 style="margin:20px 0 8px;font-size:18px;">即将上线 · 预告片单</h3>
  <ul style="padding-left:18px;margin:0 0 14px;color:#444;">
    {% for up in upcoming %}
    <li><strong>《{{ up.title }}》</strong>（预计 {{ up.date }} 上线 · {{ up.platform }}）：{{ up.points }}</li>
    {% endfor %}
  </ul>
  {% endif %}

  <div style="background:#f6ffed;border-left:4px solid #52c41a;padding:10px 12px;margin:10px 0 14px;border-radius:4px;">
    <strong>要不要看？</strong> {{ verdict }}
  </div>

  <div style="font-size:14px;color:#666;">
    今日信息来自平台公开页面，部分口碑摘录已匿名化并标注来源。如有片方希望补充海报与剧情物料，欢迎私信。
  </div>
  <div style="margin-top:12px;font-size:15px;">
    看完点个“在看”，下期带你挖掘被埋的宝藏短剧。
  </div>
</div>
